spring:
#  datasource:
#    url: jdbc:h2:mem:test;  #테스트 에선 inmemory 방식으로 로딩(spring boot 에선 별도 설정이 없으면 inmemory 방식 사용(default)
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop   # 테스트에선 create-drop 이 default
#    properties:
#      hibernate:
##        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
    #jpabook.jpashop.service.OrderService: error